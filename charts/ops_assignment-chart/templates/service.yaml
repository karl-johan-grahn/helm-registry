apiVersion: v1
kind: Service
metadata:
  name: {{ include "ops_assignment-chart.fullname" . }}
  labels:
{{ include "ops_assignment-chart.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ops_assignment-chart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
