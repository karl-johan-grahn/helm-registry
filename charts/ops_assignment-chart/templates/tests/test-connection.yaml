apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ops_assignment-chart.fullname" . }}-test-connection"
  labels:
{{ include "ops_assignment-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ops_assignment-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
